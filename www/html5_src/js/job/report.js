define(function(require, exports) {
    require("zepto");
    var api = require("../widget/api");
    var url = require("../widget/url-handle");

    var jobID = url.getParams(window.location.search).job_id;


});